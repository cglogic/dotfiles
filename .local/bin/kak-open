#!/bin/sh

if [ ! -p "$1" ]; then
	exit 0
fi

if [ -n "${WAYLAND_DISPLAY+x}" ]; then
	WW="waitwinch"
else
	WW=""
fi

FILE=$(ag --nocolor -l -g "" | sort | $WW fzy -l max -p "❯❯❯ ")

if [ ! -n "$FILE" ] || [ ! -f "$FILE" ]; then
	exit 0
fi

echo "edit $FILE" > $1
