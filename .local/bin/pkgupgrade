#!/bin/sh

if [ $(id -u) -ne 0 ] ; then
	echo "You should be root to run this command"
	exit
fi

cd /usr/ports
if [ $? -ne 0 ] ; then exit ; fi

git pull
if [ $? -ne 0 ] ; then exit ; fi

portmaster -BDa
zpool sync
cd
